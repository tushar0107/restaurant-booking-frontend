import React from "react";
// import { useSelector } from "react-redux";

const Home = () => {
  // const user = useSelector((state)=>state.user);
  
  

  return (
    <>
      <div id="home-page">
      <h3>home page</h3>
      </div>
    </>
  );
};

export default Home;
